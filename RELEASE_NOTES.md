# v0.1.0

Initial read-only developer-tools preflight: system selection, DEVELOPER_DIR override detection, lookup-only clang/macOS SDK checks, and optional full-Xcode layout check.

Value-free reports, explicit UNKNOWN semantics, no repair or compiler execution. Single zsh script runs with the stock macOS shell. Linux is fixture-test-only with an explicit unsupported live result.
